#include <iostream>
#include <array>

void PrintArray(const std::array<int, 5>& data)
{
    for (int i = 0; i < data.size(); i++)
    {
        
    }
}

int main()
{
    std::array<int, 5> data;
    // data.size();
    data[0] = 2;
    data[4] = 1;

    int dataOld[5];
    dataOld[0] = 0;

    std::cin.get();
}